# This workflow is autogenerated by xCLI and should not be manually edited.
name: Test monolith rollback
on:
  workflow_run:
    workflows: [Test monolith]
    types: [completed]
    branches: [test-monolith-gha]
jobs:
  on-success:
    runs-on: x-demo
    if: ${{ github.event.workflow_run.conclusion == 'success' }}
    steps:
      - run: echo 'The triggering workflow passed'
  on-failure:
    runs-on: x-demo
    if: ${{ github.event.workflow_run.conclusion == 'failure' }}
    steps:
      - run: echo 'failed, rollback everything'
  rollback-a:
    runs-on: x-demo
    if: ${{ github.event.workflow_run.conclusion == 'failure' }}
    steps:
      - name: "rollback a"
        run: |
          echo "rollback a with a reusable workflow"
  rollback-b:
    runs-on: x-demo
    if: ${{ github.event.workflow_run.conclusion == 'failure' }}
    steps:
      - name: "rollback b"
        run: |
          echo "rollback b with a reusable workflow"      