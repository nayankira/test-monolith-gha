# This workflow is autogenerated by xCLI and should not be manually edited.
name: Test Reusable Deployment
on:
  workflow_call:
    inputs:
      env:
        description: env
        required: true
        type: string
      app:
        description: app
        required: true
        type: string
      version:
        description: version
        required: true
        type: string

jobs:
  chekcout-deployment:
    runs-on: ubuntu-22.04
    environment: ${{ inputs.env }}
    name: checkout ${{ inputs.app }} ${{ inputs.env }} ${{ inputs.version }}
    steps:
      - name: "checkout deploy ${{ inputs.app }} ${{ inputs.env }} ${{ inputs.version }}"
        run: |
          echo "checkout deploy ${{ inputs.app }} ${{ inputs.env }} ${{ inputs.version }}"
  app-post:
    runs-on: ubuntu-22.04
    environment: ${{ inputs.env }}
    name: Post Deploy ${{ inputs.app }} ${{ inputs.env }} ${{ inputs.version }}
    steps:
      - name: "post deploy ${{ inputs.app }} ${{ inputs.env }} ${{ inputs.version }}"
        run: |
          echo "post deploy ${{ inputs.app }} ${{ inputs.env }} ${{ inputs.version }}"
